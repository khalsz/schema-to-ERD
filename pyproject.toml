[project]
name = "schema-to-erd"
version = "0.1.0"
description = "Converts JSON schema into simple ERD diagram"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "graphviz>=0.21",
    "pandas>=3.0.1",
    "pytest>=9.0.2",
]
[project.scripts]
schema-to-erd = "schema_to_erd.cli:main"
